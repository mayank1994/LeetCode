class Solution {
    public int numJewelsInStones(String J, String S) {
        
        char[] arr = S.toCharArray();
        int count=0;
        
        for(int c: arr){
            if(J.indexOf(c)!=-1)
                count++;
        }
        
        return count;
    }
}
